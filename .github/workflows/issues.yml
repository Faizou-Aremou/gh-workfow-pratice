name: Issue workflow
on: 
  issues:
    types: [opened, edited, milestoned]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: issue happened
        run:  echo "${{ toJSON(github.event) }}"

